%h1 Listing events

%table
  %tr
    %th Title
    %th Event code
    %th Soundcloud url
    %th Start time
    %th End time
    %th
    %th
    %th

  - @events.each do |event|
    %tr
      %td= event.title
      %td= event.event_code
      %td= event.soundcloud_url
      %td= event.start_time
      %td= event.end_time
      %td= link_to 'Show', event
      %td= link_to 'Edit', edit_event_path(event)
      %td= link_to 'Destroy', event, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Event', new_event_path
